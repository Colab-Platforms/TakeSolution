{
  "name": "takesolution-cms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:client && npm run build:admin && npm run merge:builds",
    "build:client": "cd frontend/client && npm install && npm run build",
    "build:admin": "cd frontend/admin && npm install && npm run build",
    "merge:builds": "node scripts/merge-builds.js"
  }
}
